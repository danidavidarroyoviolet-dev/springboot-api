I will implement the Spec-Driven Development (SDD) workflow focusing on **Fase 1** and **Fase 2** of the plan, as detailed in your request.

### A. Preparación de proyectos Maven y estructura

1. **`polizas-service`**:

   * Create directory: `C:\docker\proyect-spring\polizas-service`.

   * Create `pom.xml`: Based on `.spec/docs/polizas-service.xml`, ensuring Java 17, Spring Boot 3.x, and required dependencies (web, validation, jpa, mysql, lombok).

   * Create Directory Structure: `src/main/java/com/empresa/polizas/` with `domain`, `application`, `infrastructure`.

   * Create Main Class: `PolizasServiceApplication.java`.

   * Create `Dockerfile`: Multi-stage build (maven build -> jre alpine run).

   * Create `src/main/resources/application.yml` and `application-docker.yml`.

2. **`api-gateway`**:

   * Create directory: `C:\docker\proyect-spring\api-gateway`.

   * Create `pom.xml`: Based on `.spec/docs/api-gateway.xml`, ensuring Spring Cloud Gateway dependencies.

   * Create Main Class: `ApiGatewayApplication.java` in `com.empresa.gateway`.

   * Create `Dockerfile`: Multi-stage build.

### B. Dominio y contratos de `polizas-service`

1. **Entities (Domain)**:

   * Create `Poliza` entity in `domain` package (id, tipo, estado, vigencia, canon, prima).

   * Create `Riesgo` entity in `domain` package (id, polizaId, descripcion, estado).

   * *Note*: Will adhere strictly to `spec.md` rules.

2. **API Contracts (Application/Infrastructure)**:

   * Create DTOs in `application/dto`: `PolizaDTO`, `RiesgoDTO`, `RenovacionRequest`, `RiesgoRequest`.

   * Create Controller Interface/Class in `infrastructure/controller`: `PolizaController` defining the endpoints:

     * `GET /polizas`

     * `GET /polizas/{id}/riesgos`

     * `POST /polizas/{id}/renovar`

     * `POST /polizas/{id}/cancelar`

     * `POST /polizas/{id}/riesgos`

     * `POST /riesgos/{id}/cancelar`

3. **Business Validations**:

   * Implement basic validation logic stub or comments in the service layer reflecting rules (Individual=1 risk, etc.).

### C. Orquestación con Docker Compose

1. **`docker-compose.yml`**:

   * Create `C:\docker\proyect-spring\docker-compose.yml` based on `.spec/docs/docker-compose.yml`.

   * Services: `mysql-db`, `redis`, `polizas-service` (build context ./polizas-service), `api-gateway` (build

