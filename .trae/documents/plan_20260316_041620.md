I will update `c:\docker\proyect-spring\README.md` to accurately reflect the endpoints implemented in the project, consistent with the `spec.md` and the actual Gateway configuration.

### 1. Analysis of Current README vs. Implementation
The current `README.md` seems to be a generic template or an early draft. I need to update it to match the specific paths exposed by the API Gateway and the structure we've built.

**Key Changes:**
1.  **Base URL**: Emphasize that all access should be through the Gateway at `http://localhost:8080/api`.
2.  **Endpoints**: Update the list to match exactly what `PolizaController`, `RiesgoController` and `CoreMockController` expose, but prefixed with `/api`.
    - `GET /api/polizas`
    - `GET /api/polizas/{id}/riesgos`
    - `POST /api/polizas/{id}/renovar`
    - `POST /api/polizas/{id}/cancelar`
    - `POST /api/polizas/{id}/riesgos`
    - `POST /api/riesgos/{id}/cancelar`
    - `POST /api/core-mock/evento`
3.  **Execution Instructions**: Update to mention `docker compose up --build` as the primary way to run the full stack (DB + Redis + Services).

### 2. Execution Plan
1.  **Update README.md**: Rewrite the "Endpoints" and "Cómo ejecutar el proyecto" sections to align with the SDD and the current Docker Compose setup.

### 3. Verification
- Review the markdown content to ensure it matches the routes defined in `architecture.md` and `application.yml` of the Gateway.
