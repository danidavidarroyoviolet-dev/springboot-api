I will align the `Poliza` and `Riesgo` entities, their DTOs, the `PolizaService`, `PolizaController`, and `PolizaRepository` to use strongly-typed Enums instead of `String` for `tipo` and `estado`, exactly as requested and specified in `.Spec/spec.md`.

### 1. Analysis of the Request
The user wants to refactor the domain model to use Enums instead of Strings. Looking at the previous steps and the files, I actually already created the Enums (`TipoPoliza`, `EstadoPoliza`, `EstadoRiesgo`) and updated the entities (`Poliza`, `Riesgo`), DTOs, and Service/Controller in the previous turn! 

Wait, let me double-check the current state of the repository.

1. `Poliza.java` currently has:
```java
    @Enumerated(EnumType.STRING)
    @Column(nullable = false)
    private TipoPoliza tipo;

    @Enumerated(EnumType.STRING)
    @Column(nullable = false)
    private EstadoPoliza estado;
```
2. The Enums `TipoPoliza`, `EstadoPoliza`, `EstadoRiesgo` already exist in `com.empresa.polizas.domain`.
3. `PolizaRepository` still uses `String`:
```java
    List<Poliza> findByTipo(String tipo);
    List<Poliza> findByEstado(String estado);
    List<Poliza> findByTipoAndEstado(String tipo, String estado);
```
4. `PolizaController` takes `TipoPoliza` and `EstadoPoliza` as Enums but converts them to String to pass to the service:
```java
        String tipoStr = (tipo != null) ? tipo.name() : null;
        String estadoStr = (estado != null) ? estado.name() : null;
        List<Poliza> polizas = polizaService.listarPolizas(tipoStr, estadoStr);
```
5. `PolizaService` takes `String` for listing:
```java
    public List<Poliza> listarPolizas(String tipo, String estado) {
```

### 2. Execution Plan

The domain entities and Enums are actually already correct based on my previous actions. What I need to do to complete the requested refactor safely is update the **Repository**, **Service**, and **Controller** to use the Enums natively instead of Strings.

1.  **Update `PolizaRepository.java`**:
    - Change method signatures to accept Enums: `findByTipo(TipoPoliza tipo)`, `findByEstado(EstadoPoliza estado)`, `findByTipoAndEstado(TipoPoliza tipo, EstadoPoliza estado)`.
2.  **Update `PolizaService.java`**:
    - Change `listarPolizas` signature to accept Enums instead of Strings.
3.  **Update `PolizaController.java`**:
    - Pass the Enums directly to `polizaService.listarPolizas` instead of converting them to Strings.

This will fully align the codebase with the strongly-typed Enum requirement.